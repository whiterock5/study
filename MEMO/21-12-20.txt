form 인풋 네임 과 DTO 변수명은 일치하여야한다.
*웹에서 받는 파라미터는 늘 String 이다.

자동으로 자료형변환시킨다.

자동으로 변환시키는데, 자료형이 틀리면 null 나옴.
문자형 < - > int 형

그리고 변수명에 공백이 변수명에 들어가면 null나옴.

프레임워크

1.세팅
2.라이브러리 (5년차)
3.구조파악

파일업로드
제이슨 연동 --> 아작스
섬네일
이메일

스프링 5 - > 스프링부트 -> 안드로이드연동 , 그라드
스프링 4 - > MVC 패턴

서비스 => 디비와 모델의 인터페이스

컨트롤러 => 뷰와 모델의 컨트롤러 ,  사용자의 컨트롤

모델 - > dto 자바빈규약으로 생성해야한다.

맵핑명
전송받는방식
포워드냐 리다이렉트냐

디스패치 클래스 -> 기본 =>포워드

@컨트롤러
서비스라고 설정한 영역중에 뷰와 관련된 전송방식 요청처리

디스패치 서블릿클래스 -> 서블릿명이 디렉토리명이 될수도있다.
**메이븐 서블릿클래스

리스너 -> 이벤트핸들러 (콜백함수)

1.속성사용 : Model 인터페이스를 적용
2. 개별 파라미터 => get 메소드로전송되는 파라미터 : @RequestParam
3. 전체 DTO 클래스 적용 => 자바빈규약적용 : @ModerAttribute

1) 전체조회할떄 ArrayList 등 쓸떄 @ModelAttribute("arrayList")
2) 생략이 가능하다 @ModelAttribure (자바빈규약으로 되있을때만)
3) form(Atr atr)