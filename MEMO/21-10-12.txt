프로토타입기능 -> 동적객체가 정적 객체로 사용하는 방법

변수 = 객체 = 함수 = 메소드는 동급.

자바의 메소드영역 복습

동적객체를 정적객체로 바꾸려면

function a() {
 a는 동적객체
}

b = a();
b는 정적객체 이지만 동적객체 a 와같다.

정적객체 => 매개변수에 사용가능

alert( 동적객체를 사용하지못한다)

매개변수가 없는 함수 => 하나의 정적객체로 인식할 수있다.


new 
주소값을 통해서 실제 값을 확인했다

프로토타입
실제값으로 주소값을 확인했다.

프로토타입 기반에서만쓸수있지만
클래스 쓰는애들은안됨.

람다식 -> 함수형 언어.

함수의 매개변수는 함수
sum(call)

-> 이름이있는함수 : 자체연산 (자바스크립트 + html )

-> 익명함수 : 외부연산 (자바스크립트 + css) : 콜백함수 : 제이쿼리

객체 지향 기법 -> 상속 예외처리(오류발생해도 정상종료시키는것)