httprequset -> 각문서마다 세션부가
DAO => 쿼리문 = 공통기능 => 추출 ORM


AOP 관점 지향언어
공통기능을 추출해서 에러페이지를 만든다.
AOP예시) 로그
AOP = 가로채기 = 인터셉터

DI => 

유지할수있는 기능이 없다 -> 서버에서
클라이언트 => 쿠키

서버에서 쿠키 사용불가능.

웹브라우저 정보 유지 기능인 쿠키를 응용해서 하나 내용을 만든다.

쿠키를 통해서 세션을만든다.
세션은 쿠키의 일종이다.

클라이언트에서 유지하는 쿠키 - 쿠키
서버에서 유지하는 쿠키 - 세션
