MVC

M - 모델 DTO
V - 뷰 JSP
C - 컨트롤러 서블릿

서블릿 = 클래스 => 컨트롤러 (라이브러리 사용에 한계가 없다.)
	jsp => 뷰 (라이브러리를 사용하는것이 한계를 가지고있다.)

리다이렉트 -리퀘스트 경로 바뀌는형태  (새로운 리퀘스트객체를생성) == 상태줄이 변경된다.
포워드 - 리퀘스트 경로가 바뀌지않는형태 

쿠키,세션 - 파라미터 저장

웹에서는 리퀘스트 객체로 전달.

상태줄에 표현하겠다 = > 리퀘스트 객체에서 있는 주소값을 실제값으로 표현하겠다.


응답 403 404 로 표현?

HTTP 메소드
GET :: !
Post :: get으로 받아서 일괄처리하는 것


