웹 브라우저 -> 셋 겟 -> DTO -> 자바 
			-> DAO
웹브라우저로 파라미터를 넘겨주는태그 폼태그

JSON은  언어마다 다르다.


예전엔 xml로 다했는데 모바일연동, 로그인관련
댓글관련 실시간 검색관련 비정형데이터등 json으로함
실시간에 관련된것

컬렉션 List -> 메모리에서 테이블의 구조

웹브라우저 -> 테이블의 구조 xml or json

파싱 -> 2진데이터

json 주석 금지

프로퍼티에s 붙으면 속성 = 자바의 프로퍼티


키 == 폼태그의 name값

유효성 검사할때는 반드시 name의 값으로 접근해야 된다



p태그 순서따지면
스크립트로 따로불러올땐?


오라클에서 스키마 =하나의 문서
xml 에서 dtd와 스키마는 좀다름

url : www.naver.com(디렉토리)

자바에서 url과 비슷한것은 import

스키마 = 자바와 연동을하고 자바의 특징 그대로 사용

DTD = 자바와 연동을 하고 다른형태로 사용 -> 자바와 데이터 베이스 -> DAO

call = document.getElementById("hello");
call =  $("#hello")



p 종이컵 전화기 연상
ul 종이컵 전화기 연상