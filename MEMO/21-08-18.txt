배열 = 같은자료형만 사용
리스트 = 다른자료형을 사용가능 (가변 배열)

안의 들어간 요소 ( 값 ) 은 관계가없다.

알고리즘 사용 잘함 ? =  문 사용 잘하는가? (for / if 등등)


int[] score = new int [5];
System.out.println(score);
나오는 결과값
힙에있는값 => 레퍼런스값 = 주소값


스택 => 오브젝트

오브젝트가 주소값을 가지면 인스턴스가 된다.
자바에서 객체생성이랑 주소값을 보유하고 있는 객체를 의미.

190페이지 가변화배열이지  가변배열이 아님.

중첩클래스는 앱개발자들이씀