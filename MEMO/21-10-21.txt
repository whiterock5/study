맴버변수 -> 인스턴스
생성자
메소드

HTTP 는 눈에보이는게 아니니까
바디 : 표현 ~> 바디로 전송
request 라는 컨테이너 가 존재. ->  객체가 존재. (자바랑 똑같다 파라미터를 관리)
경로를 보내준다. -> 리퀘스트객체가 가지고 보내줘야함. (헤드에서만 보임) // 상태표시줄
기본이 GET 인코딩 조절불가 (기본이라서 주소표시줄에 고정된 문법으로.)
리퀘스트객체에있는 파라미터 얻겠다.
GET  : 헤드에서 처리 파라미터가 짧으면  ! [http의 프로토콜에있는 리퀘스트에 담겨져있는 파라미터를 가져온다.] 읽기 기능밖에안됨
POST  :   파라미터가 길면 바디로이관. get 이 바디로 이관 붙여줬다 post  [ 리퀘스트 객체에 담겨져있는 파라미터의 양이 많을경우 바디에 이관 하여 모아서 보낸다] 읽기 수정 가능.
put , delete : get에서 시작.

get 메소드의 파라미터는 서버단에서 인코딩가능..

리다이렉트
포워드


용량이 아님.
양이많으면
get이 하나하나 받고 post로 한번에 전송.


파라미터가 노출이 되는것들.
회원가입과같은 보안문제있는것들은 post로

f12 로 확인가능.


인코딩을 서버가 담당. get의 

겟에서 전송된 Post 인코딩이필요


그것이 요청을 받으면 화면단에 보이는것은 이미지등 mime 을 사용 html 보여준다.

다른 파라미터를 전송할때 mail


전화번호 -> ip

전송해서 볼수있는것 html

Action ="문서"

url 경로아님
서류봉투에 담으면 url
서류하나는 uri